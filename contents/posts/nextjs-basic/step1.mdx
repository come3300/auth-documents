---
title: 🧑‍🏫 第1回：Next.jsとは何か 🚀
category: Next.js
---

## 第1章：Next.jsとは何か 🚀

### 🌟 Next.jsとは？

Next.jsは、**Reactの上に構築された本格的なWebアプリケーションフレームワーク**です。普通のReactだけでは大変な作業を、Next.jsが自動でやってくれる「便利な道具箱」だと考えてください。

**身近な例で例えると：**
- React = 材料（小麦粉、卵、砂糖）
- Next.js = パンを作る専用オーブンと道具一式

### 📚 従来のReactとの違い

**普通のReactアプリケーション：**
```javascript
// ユーザーのブラウザで全て実行される
function App() {
  const [data, setData] = useState(null);
  
  useEffect(() => {
    fetch('/api/data')  // ページを開いてから取得開始
      .then(res => res.json())
      .then(setData);
  }, []);
  
  if (!data) return <div>読み込み中...</div>;
  
  return <div>{data.message}</div>;
}
```

**Next.jsアプリケーション：**
```javascript
// サーバーでデータを事前取得してから送信
export default async function Page() {
  const data = await fetch('https://api.example.com/data')
    .then(res => res.json());
  
  // ユーザーが見る時には既に完成している
  return <div>{data.message}</div>;
}
```

**📊 どちらが早い？**
- React：ページ表示 → JavaScript読み込み → データ取得 → 表示完了（**3段階**）
- Next.js：すぐに完成したページが表示（**1段階**）

### 💡 Next.jsの主要なメリット

#### 1. **パフォーマンスが圧倒的に良い**
```javascript
// Next.jsでは画像も自動で最適化される
import Image from 'next/image';

export default function ProfilePage() {
  return (
    <div>
      {/* 自動で圧縮・遅延読み込み・サイズ調整 */}
      <Image 
        src="/profile.jpg"
        alt="プロフィール写真"
        width={300}
        height={300}
        priority // 重要な画像は優先読み込み
      />
    </div>
  );
}
```

#### 2. **SEO（検索エンジン対策）に強い**
```html
<!-- Next.jsが生成するHTML（Googleがすぐ理解できる） -->
<html>
<head>
  <title>田中太郎のブログ</title>
  <meta name="description" content="プログラミングについて書いています" />
</head>
<body>
  <h1>私のブログへようこそ</h1>
  <p>今日はNext.jsについて学びました...</p>
</body>
</html>
```

#### 3. **開発が楽になる機能がたくさん**
```javascript
// ファイルを作るだけでページができる（ルーティング自動設定）
// app/about/page.js → https://yoursite.com/about
// app/blog/page.js → https://yoursite.com/blog
// app/contact/page.js → https://yoursite.com/contact

export default function AboutPage() {
  return (
    <div>
      <h1>私について</h1>
      <p>プログラマーです</p>
    </div>
  );
}
```

### 🔧 どんなサイトに向いている？

**Next.jsが得意なサイト：**
- **企業サイト**：会社紹介、サービス案内
- **ブログ**：記事の読み込みが速い
- **ECサイト**：商品ページが検索されやすい
- **ポートフォリオサイト**：作品を美しく表示

**具体例：**
```javascript
// ECサイトの商品ページ例
export default async function ProductPage({ params }) {
  const { id } = await params;
  
  // サーバーで商品データを取得（高速）
  const product = await db.products.findUnique({
    where: { id }
  });
  
  return (
    <div>
      <h1>{product.name}</h1>
      <p>価格: ¥{product.price.toLocaleString()}</p>
      <p>{product.description}</p>
      
      {/* 購入ボタンだけはクライアント側で動作 */}
      <BuyButton productId={product.id} />
    </div>
  );
}
```

---