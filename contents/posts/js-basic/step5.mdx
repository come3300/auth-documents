---
title: 第5回： 演算子とは
category: js
---

## 演算子とは

演算子とは、値を計算したり比較したりするための記号のことです。
数学で使う `+`（足し算）や `-`（引き算）も演算子の一つです。

プログラミングでは、様々な種類の演算子を使ってデータを操作します。

{/* TODO イメージ画像 */}

### 演算子の種類

JavaScriptには、主に以下のような演算子があります。

#### 1. 算術演算子
数値の計算を行う演算子です。

| 演算子 | 意味 | 例 | 結果 |
|--------|------|-----|------|
| `+` | 足し算 | `5 + 3` | `8` |
| `-` | 引き算 | `5 - 3` | `2` |
| `*` | 掛け算 | `5 * 3` | `15` |
| `/` | 割り算 | `6 / 3` | `2` |
| `%` | 余り | `5 % 3` | `2` |
| `**` | べき乗 | `2 ** 3` | `8` |

```javascript
const a = 10;
const b = 3;

console.log(a + b);  // 13（足し算）
console.log(a - b);  // 7（引き算）
console.log(a * b);  // 30（掛け算）
console.log(a / b);  // 3.333...（割り算）
console.log(a % b);  // 1（余り）
console.log(2 ** 3); // 8（2の3乗）
```

#### 2. 代入演算子
変数に値を代入する演算子です。

| 演算子 | 意味 | 例 | 同じ意味 |
|--------|------|-----|----------|
| `=` | 代入 | `x = 5` | - |
| `+=` | 加算代入 | `x += 3` | `x = x + 3` |
| `-=` | 減算代入 | `x -= 3` | `x = x - 3` |
| `*=` | 乗算代入 | `x *= 3` | `x = x * 3` |
| `/=` | 除算代入 | `x /= 3` | `x = x / 3` |

```javascript
let score = 10;

score += 5;  // score = score + 5
console.log(score); // 15

score -= 3;  // score = score - 3
console.log(score); // 12

score *= 2;  // score = score * 2
console.log(score); // 24

score /= 4;  // score = score / 4
console.log(score); // 6
```

#### 3. 比較演算子
2つの値を比較して、`true`（真）または`false`（偽）を返します。

| 演算子 | 意味 | 例 | 結果 |
|--------|------|-----|------|
| `==` | 等しい（型変換あり） | `5 == "5"` | `true` |
| `===` | 厳密に等しい | `5 === "5"` | `false` |
| `!=` | 等しくない（型変換あり） | `5 != "3"` | `true` |
| `!==` | 厳密に等しくない | `5 !== "5"` | `true` |
| `>` | より大きい | `5 > 3` | `true` |
| `<` | より小さい | `5 < 3` | `false` |
| `>=` | 以上 | `5 >= 5` | `true` |
| `<=` | 以下 | `5 <= 3` | `false` |

```javascript
const age = 20;

console.log(age > 18);   // true（20は18より大きい）
console.log(age < 18);   // false（20は18より小さくない）
console.log(age >= 20);  // true（20は20以上）
console.log(age === 20); // true（20は厳密に20と等しい）
console.log(age !== 18); // true（20は18と等しくない）
```

**💡 `==` と `===` の違い**

```javascript
console.log(5 == "5");   // true（型変換して比較）
console.log(5 === "5");  // false（型も含めて厳密に比較）

console.log(0 == false);  // true（型変換して比較）
console.log(0 === false); // false（型が違うのでfalse）
```

**基本的には`===`を使うことが推奨されています！**

#### 4. 論理演算子
条件を組み合わせる演算子です。

| 演算子 | 意味 | 例 | 説明 |
|--------|------|-----|------|
| `&&` | AND（かつ） | `A && B` | AもBも真なら真 |
| `||` | OR（または） | `A || B` | AかBのどちらかが真なら真 |
| `!` | NOT（否定） | `!A` | Aが真なら偽、偽なら真 |

```javascript
const age = 25;
const hasLicense = true;

// AND演算子（&&）: 両方trueの時だけtrue
console.log(age >= 18 && hasLicense); // true

// OR演算子（||）: どちらか一方がtrueならtrue
console.log(age >= 18 || hasLicense); // true

// NOT演算子（!）: trueとfalseを反転
console.log(!hasLicense); // false
```

#### 5. インクリメント・デクリメント演算子
値を1増やしたり減らしたりする演算子です。

| 演算子 | 意味 | 例 | 同じ意味 |
|--------|------|-----|----------|
| `++` | 1増やす | `x++` | `x = x + 1` |
| `--` | 1減らす | `x--` | `x = x - 1` |

```javascript
let count = 0;

count++;  // count = count + 1
console.log(count); // 1

count++;
console.log(count); // 2

count--;  // count = count - 1
console.log(count); // 1
```

#### 6. 文字列演算子
文字列を結合する演算子です。

```javascript
const firstName = "太郎";
const lastName = "山田";

// +演算子で文字列を結合
const fullName = lastName + firstName;
console.log(fullName); // "山田太郎"

// 文字列と数値を結合すると文字列になる
const age = 20;
const message = "私は" + age + "歳です";
console.log(message); // "私は20歳です"
```

### 演算子を使ってみよう！

#### 例1：消費税込みの金額を計算する

```javascript
const price = 1000;      // 商品の価格
const taxRate = 0.1;     // 消費税率（10%）

const tax = price * taxRate;           // 消費税を計算
const totalPrice = price + tax;        // 税込み価格を計算

console.log("商品価格: " + price + "円");
console.log("消費税: " + tax + "円");
console.log("税込み価格: " + totalPrice + "円");

// 出力:
// 商品価格: 1000円
// 消費税: 100円
// 税込み価格: 1100円
```

#### 例2：割引後の価格を計算する

```javascript
const originalPrice = 3000;  // 元の価格
const discountRate = 0.2;    // 割引率（20%）

const discount = originalPrice * discountRate;    // 割引額
const finalPrice = originalPrice - discount;      // 割引後の価格

console.log("元の価格: " + originalPrice + "円");
console.log("割引額: " + discount + "円");
console.log("割引後の価格: " + finalPrice + "円");

// 出力:
// 元の価格: 3000円
// 割引額: 600円
// 割引後の価格: 2400円
```

#### 例3：年齢チェック

```javascript
const age = 17;
const hasParentPermission = true;

// 18歳以上、または親の許可がある場合
const canEnter = age >= 18 || hasParentPermission;

console.log("入場できますか？: " + canEnter); // true

if (canEnter) {
  console.log("入場できます！");
} else {
  console.log("入場できません。");
}
```

#### 例4：偶数・奇数の判定

```javascript
const number = 7;

// 2で割った余りが0なら偶数、それ以外は奇数
if (number % 2 === 0) {
  console.log(number + "は偶数です");
} else {
  console.log(number + "は奇数です");
}

// 出力: 7は奇数です
```

#### 例5：ポイントの計算

```javascript
let point = 100;
const purchaseAmount = 5000;

// 購入金額の5%をポイントとして加算
point += purchaseAmount * 0.05;

console.log("現在のポイント: " + point + "pt");
// 出力: 現在のポイント: 350pt
```

## 実践問題

以下の問題に挑戦してみましょう！

### 問題1: BMI計算機を作ろう
体重（kg）と身長（m）からBMIを計算するプログラムを書いてください。

BMIの計算式: `体重 ÷ (身長 × 身長)`

```javascript
const weight = 65;  // 体重（kg）
const height = 1.7; // 身長（m）

// ここにBMIを計算するコードを書いてください
const bmi = weight / (height * height);

console.log("BMI: " + bmi);
// 出力例: BMI: 22.49...
```

### 問題2: 合計点と平均点を計算しよう
3科目のテスト点数から合計点と平均点を計算してください。

```javascript
const mathScore = 85;
const englishScore = 92;
const scienceScore = 78;

// ここに合計点と平均点を計算するコードを書いてください
const total = mathScore + englishScore + scienceScore;
const average = total / 3;

console.log("合計点: " + total);
console.log("平均点: " + average);
```

### 問題3: 割引判定プログラム
購入金額が5000円以上で、かつ会員の場合に割引が適用されるプログラムを書いてください。

```javascript
const purchaseAmount = 6000;
const isMember = true;

// ここに割引が適用されるか判定するコードを書いてください
const canDiscount = purchaseAmount >= 5000 && isMember;

if (canDiscount) {
  console.log("割引が適用されます！");
} else {
  console.log("割引は適用されません。");
}
```

## まとめ

今回学んだこと:

1. **算術演算子**: `+`, `-`, `*`, `/`, `%`, `**`で計算
2. **代入演算子**: `=`, `+=`, `-=`などで値を代入
3. **比較演算子**: `>`, `<`, `===`などで値を比較
4. **論理演算子**: `&&`, `||`, `!`で条件を組み合わせ
5. **インクリメント/デクリメント**: `++`, `--`で値を増減
6. **文字列演算子**: `+`で文字列を結合

演算子を使いこなすことで、様々な計算や判定ができるようになります！
